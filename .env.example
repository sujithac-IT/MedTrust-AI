# MedTrust AI Platform Configuration

# Optional: Google Gemini API Key for online generative extraction
# If omitted or left empty, the built-in zero-dependency Offline Clinical NLP Fallback Engine runs automatically.
GEMINI_API_KEY=

# Server Configuration
HOST=0.0.0.0
PORT=8000
