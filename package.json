{
  "name": "meditrust-prototype",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python run_app.py",
    "start": "python run_app.py",
    "test": "python -m pytest tests/test_medtrust_clinical.py -v"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
